<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.SovietHouseholdAppliances.EventManager">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/eem_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/eem_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EventEquipmentManager"
        tools:ignore="AllowBackup">
        <activity android:name=".view.EventEditActivity"></activity>
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>